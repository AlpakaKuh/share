# share

A Rust utility for sharing data over lan networks.

## Installation

```bash
cargo install --git https://github.com/AlpakaKuh/share.git
```

## Usage
```bash
share -h
```

**Note:** Compilation may take a while due to optimization flags defined in `Cargo.toml` and `.cargo/config.toml`.

## Status (W.I.P)

The following features are currently under development:
- Upload functionality
- Correct error handling
- TLS (runtime AND static certs)
- Folder sharing (provide webui)
